---
import { cn } from "../../utils/cn";

const { title, subtitle, tagline, description, classes = {} } = Astro.props;

const {
  container: containerClass = "max-w-3xl",
  title: titleClass = "text-4xl md:text-5xl ",
  subtitle: subtitleClass = "text-xl",
  description: descriptionClass = "text-lg",
} = classes;
---

{
  (title || subtitle || tagline || description) && (
    <div class={cn("mb-2 md:mx-auto md:mb-4 text-center", containerClass)}>
      {tagline && (
        <p
          class="text-base text-secondary dark:text-blue-200 font-bold tracking-wide uppercase"
          set:html={tagline}
        />
      )}
      {title && (
        <h2
          class={cn(
            "font-bold leading-tighter tracking-tighter font-heading text-heading text-3xl dark:text-gray-100",
            titleClass
          )}
          set:html={title}
        />
      )}

      {subtitle && (
        <p class={cn("mt-4 text-muted", subtitleClass)} set:html={subtitle} />
      )}
      {description && (
        <p
          class={cn("mt-4 text-muted", descriptionClass)}
          set:html={description}
        />
      )}
    </div>
  )
}
